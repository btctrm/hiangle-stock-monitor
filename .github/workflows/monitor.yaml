name: Monitor BananaFingers Hiangle Stock

on:
  schedule:
    - cron: '*/10 * * * *'   # 每10分钟，可改为 */15 或 0 */2
  workflow_dispatch:

permissions:
  contents: write   # 允许更新 stock_state.json

jobs:
  check-stock:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install playwright requests
          playwright install chromium --with-deps

      - name: Run stock monitor
        run: python monitor.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Commit stock state if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add stock_state.json || true
          if git diff --cached --quiet; then
            echo "No stock state changes"
          else
            git commit -m "chore: update stock state [skip ci]"
            git push
          fi
        continue-on-error: true
